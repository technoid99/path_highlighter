# path_highlighter
Allows you to upload CSV/GeoJSON paths, then select portions of the path to output as CSV files.
You can then collate those paths into one file if you want with this [CSV joiner tool](https://technoid99.github.io/csv_joiner/) which also allows you to save as GeoJSON.
